/** @type {import('tailwindcss').Config} */
export default {
  // Enable class-based dark mode — the `dark` class on <html> controls it.
  // This is required so ThemeContext can toggle dark mode programmatically
  // and persist the user's preference in localStorage.
  darkMode: 'class',

  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],

  theme: {
    extend: {
      // MentorMinds brand colours — usable in both light & dark variants
      colors: {
        brand: {
          50:  '#eef2ff',
          100: '#e0e7ff',
          200: '#c7d2fe',
          300: '#a5b4fc',
          400: '#818cf8',
          500: '#6366f1', // primary indigo
          600: '#4f46e5',
          700: '#4338ca',
          800: '#3730a3',
          900: '#312e81',
          950: '#1e1b4b',
        },
        stellar: {
          // Stellar-inspired teal accent
          400: '#2dd4bf',
          500: '#14b8a6',
          600: '#0d9488',
        },
      },

      // Smooth theme transitions — applied globally via a CSS class
      transitionProperty: {
        theme: 'color, background-color, border-color, fill, stroke',
      },
      transitionDuration: {
        theme: '300ms',
      },
      transitionTimingFunction: {
        theme: 'ease-in-out',
      },

      // Chart colour tokens (override in dark mode via CSS variables)
      // See index.css for the actual variable definitions.
    },
  },

  plugins: [],
};